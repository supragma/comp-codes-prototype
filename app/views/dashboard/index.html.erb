<div class="mx-auto" style="width: 75%;">
You are signed in as <%= @current_user.first_name %> <%= @current_user.last_name %> (<%= @current_user.email %>) with role "<%= @current_user.role %>"
</div>
<br>
<h3>Current Jobs</h3>
<% @jobs.each do |j| %>
  <div class="card mb-lg-0 mb-4 h-100" style="margin-left:10px; margin-right:10px;">
    <div class="card-body p-5">
      <div class="mb-4">
        <%= j.name %> by <b><%= j.user.first_name %> <%= j.user.last_name %></b>
      </div>
      <p><%= j.description %></p>
      <a href="post/<%= j.id %>/"><button class="btn btn-success">Go To Job</button></a>
    </div>
  </div>
  <div>
  </div>
<% end %>

<% if @jobs.length == 0 %>
  There are no jobs. Create one.
<% end %>
<br>
<div style="margin-left:10px; margin-right: 10px; margin-bottom: 10px;">
  <a href="posts"><button class="btn btn-success" style="width:400px;">Create Job</button></a>
</div>
